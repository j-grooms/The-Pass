import React from 'react';

const ProfileButton = () => {
  return(
    <div className="nav-user-icon" style={{fontSize: '30px'}}>
      <i class="fas fa-users-cog"></i>
    </div>);
}

export default ProfileButton;
